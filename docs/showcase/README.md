# Showcase

Interactive demos live in:

```
apps/demo-web/src/showcase/
```

Start (this monorepo uses pnpm):

```bash
pnpm --filter demo-web dev
```

`manifest.json` tracks component status (`done` / `planned`).  
Static mock: `docs/mock/index.html`.
